class Photographer {
  constructor(json) {
    json && Object.assign(this, json);
  }
  // 2 functions
}

class Media {
  constructor(json) {
    json && Object.assign(this, json);
  }
  // 2 functions
}
